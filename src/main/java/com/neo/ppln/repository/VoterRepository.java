package com.neo.ppln.repository;

import com.neo.ppln.entity.Voter;
import org.springframework.stereotype.Repository;

@Repository
public interface VoterRepository extends AbstractRepository<Voter> {
}
